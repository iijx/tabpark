
export enum DialogType {
    ADD = 'ADD',
    EDIT = 'EDIT',
}

export enum ThemeEnum {
    LIGHT = "light",
    DARK = "dark"
}

export enum WebType {
    Normal,
    DEFAULT_ADD,
}

export const ALL_PIN_ID = 'ALL_PIN_ID';
